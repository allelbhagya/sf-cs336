- determinisim important when debugging

## data loader

```
data loader generates batch of sequences of training

b = 2 #batch size
l = 4 # length of sequence
x = get_batch(data, batch_size = b, sequence_ebgth = l)
z.size = size ([b,l])
```